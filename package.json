{
  "name": "forsage-cell-editor",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:pages": "VITE_BASE_PATH=/adv_calc/ vite build",
    "deploy:pages": "npm run build:pages && cp dist/index.html index.html && cp dist/assets/index-*.js dist/assets/index-*.css dist/assets/disclaimer-*.svg assets/ && rm -rf docs && cp -r dist docs && touch docs/.nojekyll",
    "preview": "vite preview",
    "preview:pages": "VITE_BASE_PATH=/adv_calc/ vite preview"
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.5.2",
    "vite": "^6.3.5"
  }
}
